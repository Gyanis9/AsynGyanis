# Net/Proxy —— 负载均衡器交来的原始四层身份（PROXY 协议 v1/v2 读侧）
set_property(GLOBAL APPEND PROPERTY AsynGyanisNetSources
        ${CMAKE_CURRENT_LIST_DIR}/ProxyProtocol.h
        ${CMAKE_CURRENT_LIST_DIR}/ProxyProtocol.cpp
)
